spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:nyangnyangbot-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=MONTH;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    baseline-on-migrate: false
    clean-disabled: true
    locations: "classpath:db/migration"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

logging:
  level:
    org:
      hibernate:
        orm.jdbc.bind: trace
